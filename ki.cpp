#include "ki.h"

std::vector<Move> Ki::calculateBestMove(Board& board, Bag& bag) {
	return std::vector<Move>{};
}

int Ki::simulateScore(const std::vector<Placement>& placements, Board& board, std::string word) {
	return 0;
}